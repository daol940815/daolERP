# 주문시스템 트랙 인수인계 (2026-08 기준)

기존 외부 ERP를 대체하는 자체 주문관리 시스템. 총괄 세션에서 1단계까지 진행했고,
2단계부터 이 트랙(별도 세션)이 이어받는다. 마이그레이션 번호는 **500번대**.

## 2단계 구현 현황 (2026-08-07, 브랜치 claude/daol-erp-order-system-phase2-5kp7rq)

**배포 상태**: 2026-08-10 main 정식 병합 완료(5cde9bb — revert의 revert 94db8e9
포함) → Vercel 운영 배포. 프리뷰 테스트·revert 이력은 해소됨.
**마이그레이션**: 500~504 실행 필요 (사용자 실행 — 미실행 시 새 화면은 안내
메시지로 동작).

구현 완료(빌드 통과):

- **마이그레이션 500** (`500_order_system_phase2.sql`, 사용자 실행 필요):
  `erp_products` 품목 마스터 / `erp_orders`에 vendor_id·contact_id·
  counselor_employee_id·created_by_employee_id / `erp_order_items.product_id` /
  `erp_order_change_requests` 수정·취소 요청.
- **품목 마스터** `/orders/products`: 목록(KPI+검색)·개별 등록·수정·중지 +
  상품 엑셀 업로드(품번 선택·품명 필수·매입처·판매가·매입가, 품번 기준 갱신).
  조회 전 직원, 변경 manager/admin. 매입처명은 purchase 별칭 자동 연결(발주서 대비).
- **주문 입력** `/orders/new`: 기존 컬럼 순서 유지 + 마스터 선택(주문처=vendors,
  담당자=contacts+신규 인라인 등록, 상담자=employees, 품번=erp_products 자동완성).
  다올직원은 로그인 자동. source='direct', 주문번호 자동 발번(`DYYMMDD-###`).
  하류 호환: bank_name·manager_name·staff_name·channel 텍스트를 FK와 병기,
  customer 별칭 upsert로 허브·수금 매칭이 수정 없이 소비.
- **주문 상세** `/orders/[id]` + **수정** `/orders/[id]/edit`(입력 폼 공용):
  당일(KST, created_at 기준)+본인 또는 manager/admin은 직접 수정·삭제,
  익일 이후 sales는 같은 폼에서 수정 요청/취소 요청 제출.
- **수정 승인** `/orders/approvals` (manager/admin): 변경 전·후 품목 비교,
  승인 시 주문에 반영(취소 승인은 품목 is_canceled + 금액 0, 행은 이력 보존).
  요청·처리 이력은 erp_order_change_requests에 before 스냅샷과 함께 남는다.
- 사이드바: 신규 주문·품목 마스터 열림, 주문 수정 승인(manager+) 추가.
  주문 현황 행 클릭 → 상세 이동.

### 테스트 피드백 반영 (2026-08-07)

- **소개자·책임자·책임자연락처 입력 제거** (사용자 지시): 주문 입력 폼에서 삭제.
  DB 컬럼은 기존 업로드 주문이 사용하므로 유지 — 상세 화면은 값이 있을 때만 표시.
- **카톤단위 배송비** (사용자 지시, 마이그레이션 501): erp_products에
  carton_unit(1카톤당 수량)·carton_shipping_fee(카톤당 배송비) 추가.
  주문 입력에서 품번 선택·갯수 변경 시 배송비 = ceil(갯수/카톤단위)×카톤배송비
  자동 채움(칸은 수정 가능). 상품 엑셀 업로드도 카톤단위·카톤배송비 컬럼 인식.

### 테스트 피드백 2차 (2026-08-07)

- 주문 입력에서 주문번호 칸 제거 (자동 발번이라 노출 불필요 — 수정 화면 상단엔 표시).
- 메모 라벨 "(주문 단위)" 삭제.
- **지점판매가/개별판매가 분리** (마이그레이션 502): 배송 형태별 가격 정책을
  품목 구분(지점/개별/샘플)에 연동.
  - 지점(한 곳 묶음 배송): sale_price=지점판매가 + 카톤 기준 배송비 자동
  - 개별(여러 곳 개별 배송): individual_sale_price=개별판매가(배송비 포함),
    배송비 0. 개별판매가 미지정(0) 품목은 지점판매가로 폴백
  - 샘플: 판매용 견본 무상 발송(사용자 확정 2026-08-11) — 판매가 0·배송비 0 자동,
    매입가는 낱개 발송이라 지점매입가+카톤외택배비로 자동 (칸은 수정 가능)
  - 구분·갯수·품번을 바꿀 때마다 규칙 재적용, 칸은 항상 수동 수정 가능(추천값)
  - 상품 엑셀: "지점판매가"(또는 "판매가")·"개별판매가" 컬럼 인식

### 원가표 실물 반영 (2026-08-08, 사용자 확정 — 마이그레이션 503)

원가표 엑셀(2,358행) 분석·전수 검증으로 확정된 구조:

- **품번 체계 확정**: `연도-페이지-상품(-옵션)` (예: 25-25-14-01). 전 행 기재, 중복 0.
- **가격 관계 (전수 검증, 예외 0)**: 개별매입가 = 지점매입가 + 카톤외택배비 /
  개별판매가 = 지점판매가 + 카톤외택배비. 개별매입가는 기존 ERP가 배송비를
  별도 표기 못해 만든 파생 개념 — **저장하지 않고 주문 입력 시 자동 계산**.
- **지점 배송비 공식 (확정)**: 꽉 찬 카톤 수 × 카톤택배비 + (나머지 낱개 있으면
  카톤외택배비 1건). 이전의 올림 방식은 과다 책정이라 폐기.
- erp_products 추가: loose_shipping_fee(카톤외택배비) · category(목차) ·
  option_name(옵션명).
- **업로드 파서 v2**: 회사 원가표를 그대로 업로드 가능 — '원가표' 시트만 읽고
  ('116 시계조명' 등 작업 시트 무시), 2행 헤더(택배비→카톤단위/카톤외) 처리,
  `*` 접두·줄바꿈 흡수. 카탈로그 제작용 컬럼(카탈로그 표기용 상품명·소비자가·
  구성·재고·카탈로그)은 무시(사용자 확인). 메모 자리채움값('1'/'0') 제외.
  개별판매가=지점판매가+카톤외 관계 어긋난 행 수를 결과에 보고(relation_mismatch).
- 매입처 123곳의 담당자·연락처·이메일이 원가표에 있음 — **3단계 발주서 메일
  발송의 원천**으로 사용 예정 (현재는 미적재).

### 부가상품 옵션 (2026-08-08, 사용자 확정 B안 — 마이그레이션 504)

- 배경: 포장지·쇼핑백 등 부자재는 매입처 상품의 옵션인데 기존 ERP에 옵션
  기능이 없어 별도 품목으로 관리해왔다. 옵션의 두 종류를 구분:
  변형 옵션(1P/2P, 6구 등)은 지금처럼 품번별 별도 품목 유지(변경 없음),
  부가 옵션(부자재)만 새 구조 적용.
- **확정 규칙**: 부자재는 같은 매입처 상품에만 사용 (특수 케이스는 일반 행
  직접 입력으로 처리).
- 구현: erp_products.is_addon(부가상품 플래그) + erp_order_items.parent_line_no
  (본 상품 행 연결). 주문 입력에서 본 상품 행의 매입처와 같은 부가상품이
  "옵션 추가" 칩으로 행 아래 노출 — 클릭 시 옵션 행이 본 상품 아래 붙는다
  (갯수는 본 상품 따라 초기화, 수정 가능). 본 상품 행 삭제 시 옵션 행도 삭제.
  데이터는 평평한 품목 행 그대로라 발주서·회계 하류 무변경.
- 원가표 업로드 시 품번 XX-01 대역(기타 부자재)을 부가상품으로 자동 표시
  (VIP·선결제 제외), 품목 마스터 화면에서 체크박스로 수정 가능.
- C안(정식 옵션 모델)은 하류 전면 개편이 필요해 보류로 결정.

### 긴 품명 표시 (2026-08-08, 사용자 확정 2안+그룹별 동일 폭)

- 시안 3안 비교(docs/mockups/주문입력_품명표시_시안.html) 후 확정:
  품명 칸을 자동 높이 칸으로 교체 — 긴 품명은 칸 안에서 줄바꿈되어 항상 전체 표시.
- 폭 정책: 전 칸 동일 폭 대신 **그룹별 동일 폭** — 금액 칸 6개(판매가·배송비·
  할인·합계·매입가·매입배송비)는 같은 폭으로 세로 정렬, 갯수는 좁게,
  텍스트 칸은 내용 기준 고정, 품명이 남는 폭 전부(colgroup + table-fixed).

### 업체/지점 분리 + 주문 목록 신규 UI (2026-08-10, 사용자 확정 — 마이그레이션 505)

- **업체(그룹) 마스터** `vendor_groups` + `vendors.group_id` (사용자 확정 —
  업체별 통계 대비). 백필: 업로드 주문 bank_name(2건 이상)으로 업체 생성,
  거래처는 주문 이력 최빈 은행명으로 자동 연결(수동 연결 우선, 덮어쓰지 않음).
  마이그레이션 파일에 드라이런·검증 쿼리 포함 — 실행 후 검증 결과 확인.
  주문 이력 없는 거래처는 미연결(단일 업체 취급). 미연결 정리 화면은 추후
  매출처 허브에서 (허브 트랙과 협의 필요).
- **주문 입력 2칸 복원**: 업체 선택 → 소속 지점 필터 (1곳이면 자동 선택),
  지점에서 바로 전체 검색도 가능(선택 시 업체 자동). 업체 칸은 선택 사항
  (단일 업체 거래처는 지점만 선택).
- direct 주문 저장: bank_name=업체명 / branch_name=지점명(업체명 접두 제거),
  customer 별칭 erp_name도 "업체 지점" 결합형 — 업로드 주문과 같은 모양.
- **주문 현황 신규 UI** (시안 v2 — docs/mockups/주문목록_시안_v2.html):
  KPI 4칸(기간 주문/총액/미수 클릭 필터/직접입력 클릭 필터) + 통합 검색
  (주문번호·주문처·담당자·다올직원 + 품목 테이블의 상품명·품번·상담자) +
  칩 필터(기간·수금·출처·선결제만·계산서 발행건만·내 주문만) +
  행내 상품 펼치기(옵션 └·구분 배지·취소 표시) + 50건 페이지네이션.
  집계는 필터 집합 메모리 계산(주문 ~1만 건 전제) — 10만 단위가 되면
  102 hub_summary_json 패턴 RPC로 이관.
- **원가 노출 정책**: 목록 펼치기·주문 상세의 매입가·마진은 manager/admin만
  (sales 숨김) — 목록 펼치기·상세 화면 모두 반영.
- **검사 상태 컬럼: 불필요 확정** (사용자 결정 2026-08-10) — 필요해지면 요청.
- **엑셀 다운로드** (사용자 지시): 현재 필터(기간·검색·칩) 그대로 품목 행 단위
  전개 형식 — 주문 정보가 각 품목 행에 반복(기존 ERP 다운로드 방식). 수량·판매가·
  배송비·할인·합계 포함, 매입가·매입배송비·매입합계·마진은 manager/admin만.
  필터 로직은 목록 API와 공용(lib/orders-portal-list.ts)이라 화면과 항상 같은 집합.

### 상담일지 기능 (2026-08-11 사용자 확정 — 설계 단계, 실물 엑셀 대기)

실무 흐름: 직원이 고객처 주문 관련 내용을 상담일지(엑셀)에 기록 → 주문 확정 시
그 내용으로 주문서 작성 (이중 입력). ERP 안에서 상담일지 작성 → 주문서 자동
전환으로 단축하기로 확정.

- **모델**: 별도 테이블(erp_consultations, 500번대) — 주문의 "느슨한 초안"
  + 상담 기록. erp_orders에 초안 상태로 넣지 않음(하류 집계 오염 방지).
- **입력**: 주문 입력 폼의 완화판 — 필수 없음, 아는 만큼만. 품목·수량·가격도
  기재하며 **원가표(품목 마스터) 기준으로 작성** (사용자 확인).
- **전환**: "주문서로 전환" 버튼 → 주문 입력 폼이 상담 내용으로 미리 채워져
  열림 → 필수값 보완 후 저장 → 주문에 상담 연결, 상담 상태 '주문 전환'.
  상담 1건 → 주문 N건 허용(분할 주문).
- **조회 범위**: 기본 본인만 (사용자 확인).
- **업무일지 연동 (사용자 목표)**: 상담일지에 거래처·담당자가 기록되므로
  "00월 00일 A지점 A담당자와 상담" 형태로 업무일지(영업일지)에 자동 기재.
  개인화면 트랙(contact_activities·/me/journal)과 공유 영역 — 병합 시 조율.
- **구현 완료 (2026-08-12, 마이그레이션 506)**: 실물 '주문상담' 시트(3,588행)
  분석 → 시안(docs/mockups/상담일지_시안.html) 확정 → 구현.
  - erp_consultations + erp_consultation_items + erp_orders.consultation_id.
    contact_activities 활동 유형에 '상담' 추가.
  - 화면: /orders/consultations 목록(상태·구분 칩, 다중 조건 검색, 본인 기본 /
    manager+ 전체 토글) · 작성/수정 폼(실물 컬럼 순서, 필수는 상담일·구분뿐,
    업체·지점·담당자 마스터+자유 입력 겸용 ComboFree, 품목은 주문과 동일 가격
    규칙 — form-shared.tsx 공용화).
  - 전환: 목록·폼의 전환 버튼 → /orders/new?consult=id 프리필(상담자=작성자,
    자유 입력분은 마스터 등록 안내 배너) → 저장 시 consultation_id 연결 +
    상담 상태 '주문전환'(수정 잠금, 추가 전환으로 분할 주문 가능).
  - 결제정보: AES-256-GCM 앱 레벨 암호화(lib/payment-crypto), 마스킹 표시 +
    본인 열람. **PAYMENT_ENC_KEY 환경변수 필요 (Vercel Production·Preview) —
    키 변경 시 기존 암호문 복호화 불가, 변경 금지.**
  - 영업일지 자동 기재: 담당자가 마스터 인물(contact_id)일 때 '상담' 활동 삽입.
    실물 시트의 최신품번 컬럼은 제거(사용자 확정), 카탈로그성 컬럼 없음.
  - **용어 정정 (사용자 2026-08-12)**: 영업일지·상담일지·업무일지는 서로 다른
    개념. 위의 영업일지 자동 기재는 잠정 구현이며, 상담일지·영업일지·업무일지
    고도화는 **별도 트랙으로 분리** — docs/consultation-journal-track.md (700번대).
    이 트랙(500번대)은 주문시스템(3단계 발주서·배송)에 집중.

### 구현하며 내린 결정 (사용자 확인 필요 항목 포함)

1. **주문처 입력은 vendors 단일 선택** — 시안의 은행/지점 2칸 구조 대신.
   vendors에 은행-지점 계층이 없어 2칸을 유지하면 자유 입력이 부활하기 때문.
   direct 주문은 bank_name=거래처명, branch_name=NULL로 병기. *(확인 요망)*
2. **UI 톤은 신규 톤**으로 구현 (실무자 의견 미수렴 상태) — 필드 구성·순서는
   기존 ERP 그대로이므로 클래식 톤 전환은 스타일 교체만으로 가능.
3. 수정 시 미수금 보존: 기수금액(총액-미수) 차감 후 재계산.
   품목 행은 삭제 후 재삽입(업로드 파서와 동일 패턴) — settlement_month는
   주문월로 재설정되므로 이월 보정된 direct 품목 수정 시 주의.
4. 품번 체계 미확정 → item_code NULL 허용(있으면 유일). 품목 마스터에 없는
   상품은 품명 자유 입력 허용(초기 마스터 공백 대비). *(마스터 정착 후 재검토)*
5. 주문 엑셀 업로드는 기존 관리 모드 `/upload`(erp-orders/import) 그대로 사용
   (병행 운용). 주문 포털에는 별도 업로드 화면을 만들지 않았다.
6. 주문 목록의 신규 UI 시안(KPI·칩 필터·행내 상품 펼치기)은 아직 미적용 —
   1단계 조회 셸 유지. 실무자 의견 수렴 후 적용.

## 배경 — 왜 만드는가 (기존 ERP의 4대 한계)

1. 주문처 수기 입력 → 오타 → 별칭 지옥 (회계 매칭 문제의 뿌리)
2. 배송 미연동 (delivery_status 전부 NULL)
3. 익일 수정 불가 → 음수 상계 주문으로 정정 (데이터 오염)
4. 검색·조회 불편

## 확정된 설계 결정

- **병행 운용**: `erp_orders.source = 'upload'(기존 ERP 엑셀) | 'direct'(자체 입력)`.
  하류(허브·수금 매칭·회계)는 수정 없이 두 소스를 함께 소비. 시범 직원 1~2명 →
  확산 → 기존 ERP 업로드 중단 순으로 전환.
- **단계**: 1단계 완료 — 주문 포털 셸(/orders, 사이드바형), 주문 현황 목록(내 주문),
  직원·계정 관리(ID 로그인, 등급 3단계), 모드 선택. / **2단계(다음)** — 품목 마스터 +
  주문 입력 + 주문·품목 엑셀 업로드 + 수정 승인 워크플로. / 3단계 — 발주서 엑셀 생성 +
  네이버 SMTP 발송(백업 CC) + 배송 관리 통합.
- **수정 정책**: 당일 자유 수정, 익일 이후는 수정 요청 → 중간 관리자(manager) 승인.
  음수 상계 주문 방식 폐지.
- **주문 입력 필수 항목**: 거래처 담당자(contacts 마스터) + 상담자(employees 마스터).
  다올직원(입력자)은 로그인 계정 자동 기록. 주문처는 vendors 마스터 선택(자유 입력 금지).
  상담자 비인명 값은 공란 정책(직원 마스터 화이트리스트 대조).
- **발주서(3단계)**: 품목의 매입처(purchase_vendor_name)별 자동 분리, 기존 엑셀 양식
  재현, 네이버 SMTP 발송. SMTP 발송분은 보낸메일함에 안 남으므로 백업 CC + 시스템
  발송 이력으로 보완.
- **미수 규칙**: direct 주문은 입금 매칭 전액 차감 (마이그레이션 107 컷오프 규칙은
  upload 주문에만 적용 — lib/vendor-hub.ts cutoffAlloc 참조).
- **UI 방향**: "입력은 기존을 닮게, 조회는 새롭게". 시안 3종이 docs/mockups/에 있음 —
  주문목록(신규 톤) / 주문입력(신규 톤) / 주문입력(클래식 기존 ERP풍).
  신규 vs 클래식 톤은 실무자 의견으로 결정(구현 비용 동일). 실무자 의견 수렴 중.
- **개인화면 트랙(300번대)과 연계**: 직원 워크스페이스(B안) 사이드바의 주문 메뉴가
  단계적으로 열리는 구조. `app/(orders)/` 영역은 개인화면 트랙과 공유 —
  main 병합 전 rebase 필수.

## 대기 중인 사용자 입력 (진행 전 확인)

1. 실무자 의견 (UI 톤·목록 화면 사용성) — 수렴 중
2. 기존 화면 해석 미답 3건: 상태 점 2개의 의미 / 검사상태 정의 / 품번(상품 마스터) 체계
3. 기존 ERP 주문상세·주문추가 화면 캡처 (입력 화면 배치를 실물에 맞추기 위함)
4. 품목 마스터용 상품 엑셀 (품번·품명·매입처·판매가·매입가) — UI 확정과 무관하게 선행 가능
5. 발주서 엑셀 양식 / 배송현황 HTML 구현물 / 네이버 SMTP 계정 / 시범 운용 직원 지정

## 관련 코드

- 포털: `app/(orders)/` (layout, orders-sidebar.tsx, page.tsx) + `app/api/orders-portal/`
- 업로드 파서(컬럼 구조의 기준): `app/api/erp-orders/import/route.ts`
- 배송상태 로직(재사용): `lib/erp-delivery-status.ts`
- 관련 마이그레이션: 103(직원 확장·source), 104(등급·login_id), 107(미수 컷오프)
- 역할·인증: `lib/user-role.ts` (sales/manager/admin, ID 로그인)

## 데이터 참고

- 기존 주문 8,630건(품목 22,950행) 전부 source='upload'. direct 주문 0건.
- 상담자(channel)는 품목 단위 실데이터 99.9% 기재, 주문 단위로 사실상 동일(0.5%만 라인별 상이).
- 담당자(manager_name) 98% 기재 — 자유 텍스트, contacts 매칭은 담당자 관리 화면의
  미확인 큐로 정리.
