# 시스템 전체 검수 보고서

작성일: 2026-06-29
브랜치: `claude/elegant-pascal-fTXWT` (최신 커밋까지 푸시 완료)
검수 범위: 분개·원장·기초잔액·세금계산서·수동분개·다운로드·T자형 UI 전반 + 운영 DB 데이터 무결성

---

## 종합 결론

**핵심 회계 엔진과 데이터는 무결성 문제 없음.** 전역 시산표(총차변=총대변)가 정확히 일치하고, 원천↔분개 정합성·멱등성·참조 무결성 모두 통과. 정적 검사(타입·린트·빌드)도 전부 통과.

남은 것은 이미 보고드린 **배포 전 보안 항목(API 인증·RLS)** 뿐이며, 기능 자체의 결함은 발견되지 않았습니다.

---

## 1. 데이터 무결성 (운영 DB 전수) — 전부 ✅

| 점검 | 결과 |
|------|------|
| 전표 수 | 180건 |
| 차변/대변 균형 | 불균형 **0** |
| 라인 2개 미만 전표 | 0 |
| 출처(source) 누락 | 0 |
| 출처 중복(멱등성 위반) | 0 |
| **전역 시산표 (총차변=총대변)** | **219,115,507원 = 219,115,507원** ✅ |
| 금액≤0·계정 누락 라인 | 0 |
| 존재하지 않는 계정 참조 | 0 |
| 존재하지 않는 거래처 참조 | 0 |

## 2. 원천 ↔ 분개 정합성 — 전부 ✅

| 원천 | 전기대상 | 분개 | 일치 |
|------|---------|------|------|
| 은행거래(확정) | 31 | 31 | ✅ |
| 법인카드(확정) | 3 | 3 | ✅ |
| 세금계산서(계정확정) | 146 | 146 | ✅ |

→ 확정된 원천은 빠짐없이 전기되고, 미확정/이체/미분류는 정확히 제외됨.

## 3. 회계 규칙 정합성 — 전부 ✅
- 카드 미지급금 라인 **전부 카드사(하나카드) 태깅** (미태깅 0)
- 은행 GL 매핑 정상 (일반→보통예금, 마이너스통장→단기차입금, 오류 0)
- 음수(수정)세금계산서 **역분개** 정상 (차↔대 반전, 균형 유지)

## 4. 정적 검사 — 전부 ✅
- TypeScript 타입체크: 통과
- ESLint: 경고/오류 0
- 프로덕션 빌드: 성공 (41개 페이지 생성)
- 폐기 함수(`derive_bank_opening_balances`)·라우트 잔존 참조: 없음
- TODO/FIXME/디버그 로그(신규 모듈): 없음
- 미커밋 변경: 없음 (전부 푸시됨)

## 5. 마이그레이션 적용 상태 — 전부 ✅
040~048 핵심 객체 전수 확인(account_ledger, card_accounts.vendor_id, 단기차입금 2002, account_opening_balances, suggest_bank_opening_balances, 부가세계정 1102, vendor_opening_balances) + 046 폐기함수 제거 확인.

---

## 관찰사항 (결함 아님 / 향후 고려)

| 항목 | 내용 | 영향 | 권고 |
|------|------|------|------|
| 성능(목록) | 분개장·원장이 기간 내 전체를 한 번에 로드(페이지네이션 없음) | 현재 180건 — 무관. 수년치 누적 시 느려질 수 있음 | 데이터 늘면 페이지네이션 검토 |
| 보통예금 기초 | 거래 순서정보(tx_time) 부재로 자동도출 불가 → 수기 확정 방식 | 의도된 설계 | 도입시점 실잔액 수기 입력 |
| 월별손익 미반영행 | 급여·감가상각 등 자료 미보유분은 '미반영' 표기 | 의도된 설계 | 자료 확보 시 반영 |

## 배포 전 필수 (기능 결함 아님 · 별도 합의됨)
- **A. API 인증 가드** — `/api` 99개 라우트 로그인 미검사
- **B. RLS 잠금** — 테이블 RLS 미설정
- **C. service_role 키 로테이션**

→ 테스트 완료 후 진행하기로 함.

---

## 최종 판정
**기능·데이터·코드 무결성: 이상 없음.** 사용 테스트 진행에 지장 없으며, 발견된 결함은 없습니다. 배포 단계에서 보안 항목(A·B·C)만 처리하면 됩니다.
